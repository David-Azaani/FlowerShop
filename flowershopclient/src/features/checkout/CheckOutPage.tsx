import { Typography } from "@mui/material";
import React from "react";

export default function CheckOutPage() {
  return (
    <Typography variant="h3">Only Logged user Should see this!</Typography>
  );
}
